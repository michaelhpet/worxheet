[ ] To immediately make worksheet available for consumption when at least 1 artifact is generated (ungenereted artifact types are disabled)
If pipeline status remains as processing, more materials can be added realtime to the UI (enabling the following artifact type tabs) - we could use a poll or event for this (only trigger listening if artifact pipeline is running)

[ ] cleanup inference dialog/wizard - should not close when user clicks on test connection

[ ] test ability to run multiple files and different types

[ ] build evaluation metrics and benchmarks based on data we have in core/logs
